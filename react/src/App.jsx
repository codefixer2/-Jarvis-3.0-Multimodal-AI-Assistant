import React from "react";

export default function App() {
  return (
    <div className="app-shell">
      <header className="app-header">
        <span className="logo">Jarvis React</span>
        <span className="badge">Future UI</span>
      </header>
      <main className="app-main">
        <p>This is a placeholder React app for future development.</p>
        <p>Run <code>npm run dev</code> inside <code>/react</code> to start.</p>
      </main>
    </div>
  );
}

