# JARVIS React UI (future)

Lightweight scaffold to build a React/Vite front-end later.

## Structure
- `public/` – static entry HTML.
- `src/` – React components and styles.

## Quick start
```bash
cd react
npm install
npm run dev
```

Then open the printed local URL from Vite.

